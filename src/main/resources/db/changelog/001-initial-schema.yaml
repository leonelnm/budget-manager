databaseChangeLog:
  - changeSet:
      id: Initial schema
      author: leonelnm
      changes:
        - sql:
            sql: > 
              CREATE TABLE `budget` (`id` INT NOT NULL AUTO_INCREMENT, `name` VARCHAR(255) NOT NULL, `amount` DECIMAL(10,2) NOT NULL, PRIMARY KEY (`id`));